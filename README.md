# Python Data Foundations

Small starter project demonstrating a clean Python setup (venv, requirements, src layout)
and a simple pandas-based script.

## Run
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python src/main.py
